@model GestaoObras.Domain.Entities.Material
@{
    ViewData["Title"] = "Apagar Material";
}

<div class="card card-outline card-danger">
  <div class="card-header">
    <h3 class="card-title mb-0">Confirmar eliminação</h3>
  </div>

  <div class="card-body">
    <p class="text-danger mb-3">
      Tem a certeza que pretende <strong>apagar</strong> este material?
    </p>

    <dl class="row mb-0">
      <dt class="col-sm-3">@Html.DisplayNameFor(m => m.Nome)</dt>
      <dd class="col-sm-9">@Html.DisplayFor(m => m.Nome)</dd>

      <dt class="col-sm-3">@Html.DisplayNameFor(m => m.Descricao)</dt>
      <dd class="col-sm-9">@Html.DisplayFor(m => m.Descricao)</dd>

      <dt class="col-sm-3">@Html.DisplayNameFor(m => m.StockDisponivel)</dt>
      <dd class="col-sm-9">@Html.DisplayFor(m => m.StockDisponivel)</dd>
    </dl>
  </div>

  <div class="card-footer d-flex gap-2">
    <form asp-action="Delete" class="d-inline">
      <input type="hidden" asp-for="Id" />
      <button type="submit" class="btn btn-danger">
        <i class="fa-solid fa-trash"></i> Apagar
      </button>
    </form>
    <a asp-action="Index" class="btn btn-outline-secondary">
      <i class="fa-solid fa-arrow-left"></i> Cancelar
    </a>
  </div>
</div>